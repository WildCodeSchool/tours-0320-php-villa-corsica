{% extends 'base.html.twig' %}

{% block title %}Edit Villa{% endblock %}

{% block content %}
    <h1>Edit Villa</h1>

    {{ include('villa/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-success btn-lg" href="{{ path('villa_index') }}">back to list</a>

    {{ include('villa/_delete_form.html.twig') }}
{% endblock %}
