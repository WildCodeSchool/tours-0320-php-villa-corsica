<form method="post" action="{{ path('villa_delete', {'id': villa.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ villa.id) }}">
    <div class="btn_villa">
    <button class="btn btn-danger btn-lg">Supprimer</button>
    </div>
</form>
