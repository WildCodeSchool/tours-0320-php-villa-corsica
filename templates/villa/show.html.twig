{% extends 'base.html.twig' %}

{% block title %}Villa{% endblock %}

{% block body %}
    <h1>Villa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ villa.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ villa.name }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ villa.location }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ villa.description }}</td>
            </tr>
            <tr>
                <th>NbRoom</th>
                <td>{{ villa.nbRoom }}</td>
            </tr>
            <tr>
                <th>NbBed</th>
                <td>{{ villa.nbBed }}</td>
            </tr>
            <tr>
                <th>NbBathroom</th>
                <td>{{ villa.nbBathroom }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ villa.capacity }}</td>
            </tr>
            <tr>
                <th>Sqm</th>
                <td>{{ villa.sqm }}</td>
            </tr>
            <tr>
                <th>PriceFrom</th>
                <td>{{ villa.priceFrom }}</td>
            </tr>
            <tr>
                <th>MinimumStay</th>
                <td>{{ villa.minimumStay }}</td>
            </tr>
            <tr>
                <th>Poster</th>
                <td>{{ villa.poster }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('villa_index') }}">back to list</a>

    <a href="{{ path('villa_edit', {'id': villa.id}) }}">edit</a>

    {{ include('villa/_delete_form.html.twig') }}
{% endblock %}
