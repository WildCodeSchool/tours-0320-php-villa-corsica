{% extends 'base.html.twig' %}

{% block title %}GoldenBook{% endblock %}

{% block content %}
    <h1>GoldenBook</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ golden_book.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ golden_book.name }}</td>
            </tr>
            <tr>
                <th>DepartureDate</th>
                <td>{{ golden_book.departureDate ? golden_book.departureDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Commentary</th>
                <td>{{ golden_book.commentary }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('golden_book_index') }}">back to list</a>

    <a href="{{ path('golden_book_edit', {'id': golden_book.id}) }}">edit</a>

    {{ include('golden_book/_delete_form.html.twig') }}
{% endblock %}
