<div class="container">
    <div class="container" class="mt-3">
    <div class="container " class="mt-3">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="container btn2_ctn">
    <button id="villa_add" class="btn btn-info btn-lg">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
    </div>
    </div>
    </div>
</div>
