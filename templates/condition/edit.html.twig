{% extends 'base.html.twig' %}

{% block title %}Modifier conditions{% endblock %}

{% block content %}
<div class="conditionNew">
    <h1>Modifier conditions</h1>

    {{ include('condition/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
<div class="ajouter_btn">
    <a class="btn btn-warning btn-lg" href="{{ path('condition_index') }}">Retour à la liste</a>
</div>
    {{ include('condition/_delete_form.html.twig') }}
    </div>
{% endblock %}
