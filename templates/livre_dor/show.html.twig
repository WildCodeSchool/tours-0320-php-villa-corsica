{% extends 'base.html.twig' %}

{% block title %}LivreDor{% endblock %}

{% block body %}
    <h1>LivreDor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livre_dor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ livre_dor.name }}</td>
            </tr>
            <tr>
                <th>Commentary</th>
                <td>{{ livre_dor.commentary }}</td>
            </tr>
            <tr>
                <th>DateArrival</th>
                <td>{{ livre_dor.dateArrival ? livre_dor.dateArrival|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('livre_dor_index') }}">back to list</a>

    <a href="{{ path('livre_dor_edit', {'id': livre_dor.id}) }}">edit</a>

    {{ include('livre_dor/_delete_form.html.twig') }}
{% endblock %}
